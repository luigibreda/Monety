export type Usuario = {
    id: string;
    token: string;
    name: string;
    email: string;
    isAdmin: boolean;
    ativo: boolean;
    userId: string;
    createdAt: string;
    updatedAt: string;
}